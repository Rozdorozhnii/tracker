model TimeEntry {
  id          Int      @id @default(autoincrement())
  date        DateTime
  project     String
  hours       Float
  description String
  createdAt   DateTime @default(now())

  @@index([date])
}